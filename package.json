{
  "name": "sonic-prints-mern",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sonic Prints — Ganesh Festival Collection 2026. MERN storefront (MongoDB, Express, React, Node).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm run server\" \"npm run client\"",
    "sitemap": "npm run sitemap --prefix client",
    "submit-sitemap": "node scripts/submit-sitemap.js",
    "build": "npm run build --prefix client",
    "upload:gcp": "node scripts/upload-to-gcp.js",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "@google-cloud/storage": "^8.0.1",
    "concurrently": "^8.2.2"
  }
}
