/* The emoji sparkle layer has been replaced by the canvas-based festive
   particle field in components/fx/FestiveParticles.jsx, which renders the
   same idea (golden motes, dust, petals, bokeh) at a fraction of the cost
   and adds pointer + scroll interaction. Re-exported here so any older
   import keeps working. */
export { default } from "./fx/FestiveParticles.jsx";
